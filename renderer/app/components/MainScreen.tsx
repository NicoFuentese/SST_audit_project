"use client";

// Placeholder — se reemplaza en el próximo paso por la pantalla principal real
// (sección 12 del Planning.md: "Grabar reunión" / "Cargar archivo de audio").
export default function MainScreen() {
  return (
    <div className="flex min-h-screen items-center justify-center px-4">
      <p className="text-sm text-neutral-500">Pantalla principal — próximamente.</p>
    </div>
  );
}
